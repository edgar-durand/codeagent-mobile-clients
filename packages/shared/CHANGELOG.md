# Changelog

All notable changes to `@codeam/shared` are documented here.

This package versions in lockstep with the unified client release line — a `vX.Y.Z` tag in [`codeagent-mobile-clients`](https://github.com/edgar-durand/codeagent-mobile-clients) publishes it alongside `codeam-cli` and the IDE plugins.
