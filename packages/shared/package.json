{
  "name": "@codeagent/shared",
  "version": "2.0.0",
  "private": true,
  "description": "Pure-TypeScript modules shared by codeam-cli and the VS Code extension: chrome parsing, model pricing tables, context-window sizes. Bundled into each consumer at build time.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "license": "MIT",
  "files": [
    "src"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vitest": "^3.2.4"
  }
}
