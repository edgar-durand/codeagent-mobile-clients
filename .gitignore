node_modules/
dist/
dist-test/
.vscode-test/
*.tsbuildinfo

# Environment / secrets
.env
.env.local
.env.*.local

# IDE
.idea/
*.iml
.vscode/

# OS
.DS_Store
Thumbs.db

# JetBrains plugin build
apps/jetbrains-plugin/build/
apps/jetbrains-plugin/.gradle/
apps/jetbrains-plugin/.kotlin/
apps/jetbrains-plugin/.intellijPlatform/

# VS Code plugin build artifacts
apps/vsc-plugin/*.vsix

# CLI build artifacts
apps/cli/*.tgz
# Transient pack tarball the host-agent Docker E2E copies into its build
# context (cleaned up in afterAll; ignored so a crashed run can't commit it).
apps/cli/__tests__/docker/*.tgz
# Same deal for the test-only driver bundles the integration tests copy into
# that build context (mcp-shim-driver.js IS checked in — these two are not).
apps/cli/__tests__/docker/headroom-runner-driver.js
apps/cli/__tests__/docker/agent-install-driver.js

# Logs
*.log
npm-debug.log*

# Coverage
coverage/

# Local worktrees (superpowers/subagent-driven-development)
.worktrees/

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/proxieddb/
